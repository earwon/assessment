# Coding challenge 

## Getting Started

### Laravel Setup
```bash
composer install
cp .env.example .env
php artisan key:generate
php artisan migrate --seed
```
# Use this command to run the backend
```bash 
php artisan serve
```